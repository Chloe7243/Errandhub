{
  "name": "errandhub",
  "private": true,
  "workspaces": [
    "apps/*",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run start --workspace=apps/frontend\" \"npm run dev --workspace=apps/backend\" ",
    "backend": "npm run dev --workspace=apps/backend",
    "frontend": "npm run start --workspace=apps/frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.0.0"
  }
}
